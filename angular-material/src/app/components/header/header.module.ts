import { NgModule } from '@angular/core';

import { MatToolbarModule } from '@angular/material';
const HeaderMaterial = [
  MatToolbarModule
]

@NgModule({
  declarations: [],
  imports: [HeaderMaterial],
  exports: [HeaderMaterial]
})
export class HeaderModule { }
